# Scratch

Disposable working memory for one bounded rite.

## Current Subgoal

Autonomous frontier turnover after exact `16407f7e` c006 endurance.

## Current mechanism

1. `ingest_resident_self_domain_pressure` creates pressure only for an admitted
   model-direction consideration request, the oldest unlaunched proposal
   Modeling request, or admitted Persona feedback consideration.
2. Heartbeat owns oldest-pending pressure selection and atomically consumes it
   into one exact grant.
3. Resident Self launches the coordinator against the typed request. Typed
   worker output is sufficient to settle that grant.
4. The coordinator's own status derivation then owns the next internal action,
   but no nervous-system owner currently converts that exact safe continuation
   into another pressure.

Actual c006 proves the seam: proposal Modeling job
`5ff2704b-64eb-42f1-9ca4-ca7144e33ae4` fulfilled and Self closed the grant at
revision 26. Exact packaged coordinator status now says
`reviewModelingResult`; runtime has no actionable Hands or Eyes frontier,
Imagination eligibility is empty, planning is terminal, and resident pressure
is empty. Releasing the brake cannot advance the machine.

## Authority map before implementation

- Owner: Self's source-grounded coordinator decision owns which internal
  cognition transition is next. The exact runtime result/request/state revision
  own the decision's identity. Heartbeat owns only delivery order.
- Inputs: authenticated runtime identity/thread, exact current RepoModel and
  admission receipt, typed worker results and launch bindings, acceptance or
  supersession receipts, current coordinator state revision, and brake.
- Outputs: at most one idempotent internal-cognition continuation pressure
  naming the exact decision basis. It may launch only the already-derived safe
  coordinator action.
- Derived state: rendered status, role-board prose, Heartbeat pulse artifacts,
  and provider rows are observations and cannot create continuation authority.
- Forbidden writers: timers, generic idle polling, old lane status, operator
  projections, and resident lifecycle retention cannot invent or retry work.
  Hands, Persona, deployment, and external consequence actions are excluded
  from autonomous continuation.
- Shared paths: proposal Modeling review/admission, Eyes gathering, causal
  Modeling, Imagination planning, Mind review, and typed decision commit must
  use the same decision identity and single-consumption pressure primitive.
- Cut line: do not add a blind coordinator wake loop or treat every completed
  worker as a generic retry. If coordinator decision derivation cannot be made
  a narrow core-owned typed projection, stop and move the owner rather than
  copying binary status logic into resident Self.
- Verification: actual c006 remains braked. First prove on copied exact state
  that the stranded result creates one continuation, settles once, advances to
  a genuinely new causal decision, never emits Hands/Persona authority, and
  cannot resurrect after retention or restart.
